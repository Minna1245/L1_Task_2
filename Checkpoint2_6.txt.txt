Which git command would you use to:
a. View the history of the repository
	git log
b. If there are outstanding commits to do
	git commit -m "inserting comment here"
c. Start a new repository
	git init
